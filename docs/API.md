# Restaurant SaaS API Reference

REST API for restaurant & café management — Egypt & MENA market.

**Base URL:** `{{base_url}}` (default: `http://localhost:8000/api/v1`)

> Generated by `tools/generate-api-documentation.php`. Do not edit manually.

---

## Introduction

This API powers multi-tenant restaurant operations: POS, delivery, inventory, loyalty, and platform administration. All responses are JSON.

Supported locales: `ar` (Arabic, default for Egypt) and `en`.

---

## Authentication

### Tenant users (owners, managers, staff)

1. **Register** via `POST /onboarding/register` (no auth) or log in via `POST /auth/login`.
2. Receive a Sanctum Bearer token: `Authorization: Bearer {token}`.
3. Resolve tenant context on every request using one of:
   - Subdomain host: `{subdomain}.restoapp.eg`
   - Verified custom domain
   - Header: `X-Tenant-Subdomain: {subdomain}` (required for mobile/API clients without subdomain DNS)

### Device login (waiter / cashier / kitchen)

- `POST /auth/device/login` — PIN + branch, requires tenant header
- `POST /auth/device/kitchen` — kitchen display secret, requires tenant header

### Platform admin

- `POST /admin/auth/login` → use `Authorization: Bearer {admin_token}`
- No tenant header required

### Email verification

Most tenant routes require verified email (`verified.email` middleware). Verify via `GET /auth/verify-email` link or resend with `POST /auth/email/verification-notification`.

---

## Tenant Resolution

Priority order (see `TenantResolver`):

1. Authenticated user's tenant (from token)
2. Verified custom domain (`Host` header)
3. `X-Tenant-Subdomain` header
4. Subdomain extracted from `{subdomain}.{base_domain}`
5. First subdomain label from host

Failure returns:

```json
{
  "errors": [{ "message": "Tenant not found.", "code": "TENANT_NOT_FOUND" }]
}
```

---

## Response Envelope

### Success

```json
{
  "data": { "id": 1, "name": "مطعم النيل" },
  "meta": { "message": "OK" }
}
```

### Paginated success

```json
{
  "data": [{ "id": 1, "name": "كشري" }],
  "meta": {
    "current_page": 1,
    "last_page": 3,
    "per_page": 25,
    "total": 62,
    "message": "OK"
  }
}
```

### Error

```json
{
  "errors": [
    { "message": "Validation failed.", "code": "VALIDATION_ERROR", "field": "email" }
  ]
}
```

---

## Error Codes

| HTTP | Code | Description |
|------|------|-------------|
| 401 | `UNAUTHENTICATED` | Missing or invalid Bearer token |
| 401 | `INVALID_CREDENTIALS` | Email/password login failed |
| 401 | `INVALID_PIN` | Device PIN login failed |
| 401 | `INVALID_SIGNATURE` | Webhook signature verification failed |
| 403 | `FORBIDDEN` | Insufficient role or permission |
| 403 | `ACCOUNT_SUSPENDED` | Tenant subscription suspended |
| 404 | `NOT_FOUND` | Resource or route not found |
| 404 | `TENANT_NOT_FOUND` | Tenant could not be resolved from host or header |
| 402 | `FEATURE_NOT_AVAILABLE` | Plan does not include required feature |
| 402 | `PLAN_LIMIT_EXCEEDED` | Plan limit reached (branches, users, orders) |
| 422 | `VALIDATION_ERROR` | Request validation failed (includes field) |
| 400 | `ERROR` | Generic client error |
| 400 | `TENANT_REQUIRED` | X-Tenant-Subdomain header required |

---

## Plan Features

| Feature | Starter | Growth | Pro | Enterprise |
|---------|:-------:|:------:|:---:|:----------:|
| `aggregator_analytics` | — | — | — | ✓ |
| `ai_reports` | — | — | — | ✓ |
| `audit_log` | — | — | ✓ | ✓ |
| `customers` | — | ✓ | ✓ | ✓ |
| `daily_reports` | ✓ | ✓ | ✓ | ✓ |
| `delivery` | — | ✓ | ✓ | ✓ |
| `eta_invoice` | ✓ | ✓ | ✓ | ✓ |
| `inventory` | — | — | ✓ | ✓ |
| `kitchen_display` | ✓ | ✓ | ✓ | ✓ |
| `loyalty` | — | — | — | ✓ |
| `multi_branch` | — | — | — | ✓ |
| `pos` | ✓ | ✓ | ✓ | ✓ |
| `qr_menu` | — | ✓ | ✓ | ✓ |
| `recipe_costing` | — | — | ✓ | ✓ |
| `riders` | — | ✓ | ✓ | ✓ |
| `staff_shifts` | — | — | ✓ | ✓ |
| `suppliers` | — | — | ✓ | ✓ |
| `waste_log` | — | — | ✓ | ✓ |
| `whatsapp_marketing` | — | — | — | ✓ |
| `whatsapp_ordering` | — | ✓ | ✓ | ✓ |

---

## Endpoints

### Auth

#### `POST` /api/v1/auth/device/kitchen

Kitchen display device login using a shared branch secret.

- **Auth:** Tenant header only
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `device_secret` | `required, string` |
| `device_name` | `nullable, string, max:100` |

```json
{
    "branch_id": "{{branch_id}}",
    "device_secret": "kitchen-secret-12345678",
    "device_name": "تابلت الكاشير ١"
}
```

---

#### `POST` /api/v1/auth/device/login

Branch-scoped PIN login for waiters and cashiers. Requires tenant resolution via header or subdomain host.

- **Auth:** Tenant header only
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `pin` | `required, string, digits:4` |
| `device_name` | `nullable, string, max:100` |

```json
{
    "branch_id": "{{branch_id}}",
    "pin": "1234",
    "device_name": "تابلت الكاشير ١"
}
```

---

#### `POST` /api/v1/auth/email/verification-notification

Send Verification Notification — auth/email/verification-notification

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/auth/forgot-password

Forgot Password — auth/forgot-password

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `email` | `required, email` |

```json
{
    "email": "owner@nilerestaurant.eg"
}
```

---

#### `POST` /api/v1/auth/login

Login — auth/login

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `email` | `required, email` |
| `password` | `required, string, min:8` |
| `device_name` | `nullable, string, max:100` |

```json
{
    "email": "owner@nilerestaurant.eg",
    "password": "SecurePass123!",
    "device_name": "تابلت الكاشير ١"
}
```

---

#### `GET` /api/v1/auth/me

Me — auth/me

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/auth/reset-password

Reset Password — auth/reset-password

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `email` | `required, email` |
| `token` | `required, string` |
| `password` | `required, string, min:8, max:100, confirmed` |

```json
{
    "email": "owner@nilerestaurant.eg",
    "token": "{{qr_token}}",
    "password": "SecurePass123!"
}
```

---

#### `DELETE` /api/v1/auth/token

Logout — auth/token

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/auth/verify-email

Verify Email — auth/verify-email

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

### Delivery

#### `GET` /api/v1/customers

Index — customers

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `customers`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `phone` | `nullable, string, max:20` |
| `search` | `nullable, string, max:100` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/customers

Store — customers

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `customers`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `phone` | `required, string, max:20` |
| `name` | `nullable, string, max:100` |
| `default_address` | `nullable, string, max:500` |

```json
{
    "phone": "+201012345678",
    "name": "Downtown Branch",
    "default_address": "مثال"
}
```

---

#### `GET` /api/v1/customers/{customer}

Show — customers/{customer}

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `customers`
- **Path params:** `{customer}`

---

#### `PATCH` /api/v1/customers/{customer}

Update — customers/{customer}

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `customers`
- **Path params:** `{customer}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `nullable, string, max:100` |
| `default_address` | `nullable, string, max:500` |

```json
{
    "name": "Downtown Branch",
    "default_address": "مثال"
}
```

---

#### `GET` /api/v1/customers/{customer}/orders

Orders — customers/{customer}/orders

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `customers`
- **Path params:** `{customer}`

---

#### `POST` /api/v1/orders/{order}/assign-rider

Assign — orders/{order}/assign-rider

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `riders`
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `rider_id` | `required, integer, exists:users,id` |

```json
{
    "rider_id": "{{rider_id}}"
}
```

---

#### `PATCH` /api/v1/orders/{order}/delivery-status

Update Delivery Status — orders/{order}/delivery-status

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `riders`
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `status` | `required, in:assigned,picked_up,en_route,delivered,cancelled` |

```json
{
    "status": "assigned"
}
```

---

#### `GET` /api/v1/riders

Index — riders

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `riders`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/riders/deliveries

My Deliveries — riders/deliveries

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `riders`
- **Path params:** _None_

---

### Delivery · QR Menu

#### `GET` /api/v1/qr/{token}/menu

Public QR menu for a table token. No authentication required.

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{token}`

---

#### `POST` /api/v1/qr/{token}/orders

Place a dine-in or takeaway order from the public QR menu.

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{token}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `items` | `required, array, min:1` |
| `items.*.menu_item_id` | `required, integer` |
| `items.*.quantity` | `required, integer, min:1` |
| `items.*.notes` | `nullable, string, max:255` |
| `customer_name` | `nullable, string, max:100` |
| `customer_phone` | `nullable, string, max:20` |
| `notes` | `nullable, string, max:500` |
| `table_label` | `nullable, string, max:50` |
| `fulfillment_type` | `nullable, in:takeaway,delivery` |
| `delivery_address` | `required_if:fulfillment_type,delivery, nullable, string, max:500` |

```json
{
    "items": [
        {
            "menu_item_id": 1,
            "quantity": 2,
            "notes": "كشري"
        },
        {
            "menu_item_id": 2,
            "quantity": 1,
            "notes": "فول"
        }
    ],
    "customer_name": "أحمد محمود",
    "customer_phone": "+201012345678",
    "notes": "بدون بصل",
    "table_label": "طاولة ٥",
    "fulfillment_type": "takeaway",
    "delivery_address": "١٢ شارع التحرير، الدقي، الجيزة"
}
```

---

### Intelligence

#### `GET` /api/v1/analytics/aggregators

Compare — analytics/aggregators

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `aggregator_analytics`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `start_date` | `nullable, date` |
| `end_date` | `nullable, date, after_or_equal:start_date` |

---

#### `GET` /api/v1/loyalty/customers/{customer}

Show — loyalty/customers/{customer}

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `loyalty`
- **Path params:** `{customer}`

---

#### `POST` /api/v1/loyalty/customers/{customer}/redeem

Redeem — loyalty/customers/{customer}/redeem

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `loyalty`
- **Path params:** `{customer}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `points` | `required, integer, min:1` |
| `notes` | `nullable, string, max:255` |

```json
{
    "points": 100,
    "notes": "بدون بصل"
}
```

---

#### `POST` /api/v1/marketing/broadcast

Broadcast — marketing/broadcast

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `whatsapp_marketing`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `template_name` | `required, string, max:100` |
| `segment` | `required, in:all,inactive_30d,high_spenders,recent_visitors` |
| `parameters` | `nullable, array` |
| `parameters.*` | `string, max:255` |

```json
{
    "template_name": "مثال",
    "segment": "all",
    "parameters": []
}
```

---

#### `GET` /api/v1/marketing/campaigns

Index — marketing/campaigns

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `whatsapp_marketing`
- **Path params:** _None_

---

#### `GET` /api/v1/marketing/segments

Segments — marketing/segments

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `whatsapp_marketing`
- **Path params:** _None_

---

#### `GET` /api/v1/reports/ai-summary

Weekly — reports/ai-summary

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `ai_reports`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `week_start` | `nullable, date` |
| `narrative` | `nullable, boolean` |

---

### Inventory

#### `GET` /api/v1/inventory/ingredients

Index — inventory/ingredients

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `active` | `nullable, boolean` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/inventory/ingredients

Store — inventory/ingredients

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `name_ar` | `required, string, max:100` |
| `name_en` | `nullable, string, max:100` |
| `unit` | `required, in:kg,g,l,ml,piece` |
| `current_stock` | `nullable, numeric, min:0` |
| `reorder_level` | `nullable, numeric, min:0` |
| `unit_cost` | `nullable, numeric, min:0` |

```json
{
    "branch_id": "{{branch_id}}",
    "name_ar": "كشري",
    "name_en": "Koshary",
    "unit": "kg",
    "current_stock": 25,
    "reorder_level": 10,
    "unit_cost": 45
}
```

---

#### `GET` /api/v1/inventory/ingredients/{ingredient}

Show — inventory/ingredients/{ingredient}

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** `{ingredient}`

---

#### `PATCH` /api/v1/inventory/ingredients/{ingredient}

Update — inventory/ingredients/{ingredient}

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** `{ingredient}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `sometimes, string, max:100` |
| `name_en` | `nullable, string, max:100` |
| `unit` | `sometimes, in:kg,g,l,ml,piece` |
| `reorder_level` | `sometimes, numeric, min:0` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "unit": "kg",
    "reorder_level": 10,
    "is_active": true
}
```

---

#### `GET` /api/v1/inventory/low-stock

Low Stock — inventory/low-stock

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/inventory/movements

Index — inventory/movements

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `ingredient_id` | `nullable, integer` |
| `type` | `nullable, in:purchase,waste,adjustment` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/inventory/movements

Store — inventory/movements

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `ingredient_id` | `required, integer` |
| `type` | `required, in:purchase,waste,adjustment` |
| `quantity` | `required, numeric, min:0.001` |
| `unit_cost` | `nullable, numeric, min:0` |
| `notes` | `nullable, string, max:255` |
| `direction` | `nullable, in:in,out` |

```json
{
    "ingredient_id": "{{ingredient_id}}",
    "type": "purchase",
    "quantity": 2,
    "unit_cost": 45,
    "notes": "بدون بصل",
    "direction": "in"
}
```

---

#### `GET` /api/v1/inventory/purchase-orders

Index — inventory/purchase-orders

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `suppliers`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `status` | `nullable, string, max:50` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/inventory/purchase-orders

Create a draft purchase order for a supplier.

- **Auth:** Bearer token + tenant header
- **Permissions:** `suppliers.manage`
- **Plan features:** `suppliers`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `supplier_id` | `required, integer` |
| `notes` | `nullable, string` |
| `items` | `required, array, min:1` |
| `items.*.ingredient_id` | `required, integer` |
| `items.*.quantity` | `required, numeric, min:0.001` |
| `items.*.unit_cost` | `required, numeric, min:0` |

```json
{
    "branch_id": "{{branch_id}}",
    "supplier_id": "{{supplier_id}}",
    "notes": "بدون بصل",
    "items": [
        {
            "ingredient_id": 1,
            "quantity": 5,
            "unit_cost": 12.5
        },
        {
            "ingredient_id": 2,
            "quantity": 2,
            "unit_cost": 8
        }
    ]
}
```

---

#### `GET` /api/v1/inventory/purchase-orders/{purchaseOrder}

Show — inventory/purchase-orders/{purchaseOrder}

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `suppliers`
- **Path params:** `{purchaseOrder}`

---

#### `POST` /api/v1/inventory/purchase-orders/{purchaseOrder}/receive

Receive goods against a submitted purchase order.

- **Auth:** Bearer token + tenant header
- **Permissions:** `suppliers.manage`
- **Plan features:** `suppliers`
- **Path params:** `{purchaseOrder}`

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/inventory/purchase-orders/{purchaseOrder}/submit

Submit purchase order to supplier for fulfillment.

- **Auth:** Bearer token + tenant header
- **Permissions:** `suppliers.manage`
- **Plan features:** `suppliers`
- **Path params:** `{purchaseOrder}`

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/inventory/stock-counts

Index — inventory/stock-counts

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `status` | `nullable, string, max:50` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/inventory/stock-counts

Store — inventory/stock-counts

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `notes` | `nullable, string, max:255` |

```json
{
    "branch_id": "{{branch_id}}",
    "notes": "بدون بصل"
}
```

---

#### `GET` /api/v1/inventory/stock-counts/{stockCount}

Show — inventory/stock-counts/{stockCount}

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** `{stockCount}`

---

#### `POST` /api/v1/inventory/stock-counts/{stockCount}/cancel

Cancel — inventory/stock-counts/{stockCount}/cancel

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** `{stockCount}`

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/inventory/stock-counts/{stockCount}/complete

Complete — inventory/stock-counts/{stockCount}/complete

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** `{stockCount}`

**Request body**

_None_

```json
{}
```

---

#### `PUT` /api/v1/inventory/stock-counts/{stockCount}/lines

Upsert Line — inventory/stock-counts/{stockCount}/lines

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** `{stockCount}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `ingredient_id` | `required, integer` |
| `counted_quantity` | `required, numeric, min:0` |

```json
{
    "ingredient_id": "{{ingredient_id}}",
    "counted_quantity": 2
}
```

---

#### `GET` /api/v1/inventory/suppliers

Index — inventory/suppliers

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `suppliers`
- **Path params:** _None_

---

#### `POST` /api/v1/inventory/suppliers

Store — inventory/suppliers

- **Auth:** Bearer token + tenant header
- **Permissions:** `suppliers.manage`
- **Plan features:** `suppliers`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `required, string, max:150` |
| `phone` | `nullable, string, max:20` |
| `email` | `nullable, email, max:150` |
| `address` | `nullable, string, max:500` |

```json
{
    "name": "Downtown Branch",
    "phone": "+201012345678",
    "email": "owner@nilerestaurant.eg",
    "address": "منطقة المعادي، القاهرة"
}
```

---

#### `PATCH` /api/v1/inventory/suppliers/{supplier}

Update — inventory/suppliers/{supplier}

- **Auth:** Bearer token + tenant header
- **Permissions:** `suppliers.manage`
- **Plan features:** `suppliers`
- **Path params:** `{supplier}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:150` |
| `phone` | `nullable, string, max:20` |
| `email` | `nullable, email, max:150` |
| `address` | `nullable, string, max:500` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name": "Downtown Branch",
    "phone": "+201012345678",
    "email": "owner@nilerestaurant.eg",
    "address": "منطقة المعادي، القاهرة",
    "is_active": true
}
```

---

#### `GET` /api/v1/inventory/transfers

Index — inventory/transfers

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`, `multi_branch`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `POST` /api/v1/inventory/transfers

Store — inventory/transfers

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`, `multi_branch`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `from_branch_id` | `required, integer` |
| `to_branch_id` | `required, integer, different:from_branch_id` |
| `ingredient_id` | `required, integer` |
| `quantity` | `required, numeric, min:0.001` |
| `notes` | `nullable, string, max:255` |

```json
{
    "from_branch_id": 1,
    "to_branch_id": 1,
    "ingredient_id": "{{ingredient_id}}",
    "quantity": 2,
    "notes": "بدون بصل"
}
```

---

#### `GET` /api/v1/menu/items/{item}/cost

Cost — menu/items/{item}/cost

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** `{item}`

---

#### `GET` /api/v1/menu/items/{item}/recipe

Show — menu/items/{item}/recipe

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.view`
- **Plan features:** `inventory`
- **Path params:** `{item}`

---

#### `PUT` /api/v1/menu/items/{item}/recipe

Sync — menu/items/{item}/recipe

- **Auth:** Bearer token + tenant header
- **Permissions:** `inventory.manage`
- **Plan features:** `inventory`
- **Path params:** `{item}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `lines` | `required, array, min:1` |
| `lines.*.ingredient_id` | `required, integer` |
| `lines.*.quantity` | `required, numeric, min:0.0001` |

```json
{
    "lines": [
        {
            "ingredient_id": "{{ingredient_id}}",
            "quantity": 2
        }
    ]
}
```

---

### POS

#### `GET` /api/v1/invoices/failed

Failed — invoices/failed

- **Auth:** Bearer token + tenant header
- **Permissions:** `payments.process`
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/invoices/{invoice}

Show — invoices/{invoice}

- **Auth:** Bearer token + tenant header
- **Permissions:** `payments.process`
- **Plan features:** _None_
- **Path params:** `{invoice}`

---

#### `POST` /api/v1/invoices/{invoice}/resubmit

Resubmit — invoices/{invoice}/resubmit

- **Auth:** Bearer token + tenant header
- **Permissions:** `payments.process`
- **Plan features:** _None_
- **Path params:** `{invoice}`

**Request body**

_None_

```json
{}
```

---

#### `PATCH` /api/v1/kitchen/items/{item}/done

Mark Done — kitchen/items/{item}/done

- **Auth:** Bearer token + tenant header
- **Permissions:** `kitchen.update`
- **Plan features:** `kitchen_display`
- **Path params:** `{item}`

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/kitchen/queue

Queue — kitchen/queue

- **Auth:** Bearer token + tenant header
- **Permissions:** `kitchen.view`
- **Plan features:** `kitchen_display`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/menu/categories

Index — menu/categories

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.view`
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/menu/categories

Store — menu/categories

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.create`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `required, string, max:100` |
| `name_en` | `nullable, string, max:100` |
| `description_ar` | `nullable, string` |
| `sort_order` | `integer, min:0` |
| `branch_id` | `nullable, integer` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "sort_order": 1,
    "branch_id": "{{branch_id}}"
}
```

---

#### `DELETE` /api/v1/menu/categories/{category}

Destroy — menu/categories/{category}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.delete`
- **Plan features:** _None_
- **Path params:** `{category}`

---

#### `GET` /api/v1/menu/categories/{category}

Show — menu/categories/{category}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.view`
- **Plan features:** _None_
- **Path params:** `{category}`

---

#### `PATCH` /api/v1/menu/categories/{category}

Update — menu/categories/{category}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{category}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `sometimes, string, max:100` |
| `name_en` | `nullable, string, max:100` |
| `description_ar` | `nullable, string` |
| `sort_order` | `sometimes, integer, min:0` |
| `is_visible` | `sometimes, boolean` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "sort_order": 1,
    "is_visible": true
}
```

---

#### `PUT` /api/v1/menu/categories/{category}

Update — menu/categories/{category}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{category}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `sometimes, string, max:100` |
| `name_en` | `nullable, string, max:100` |
| `description_ar` | `nullable, string` |
| `sort_order` | `sometimes, integer, min:0` |
| `is_visible` | `sometimes, boolean` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "sort_order": 1,
    "is_visible": true
}
```

---

#### `GET` /api/v1/menu/items

Index — menu/items

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `category_id` | `nullable, integer` |
| `available_only` | `nullable, boolean` |

---

#### `POST` /api/v1/menu/items

Store — menu/items

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.create`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `category_id` | `required, integer` |
| `name_ar` | `required, string, max:150` |
| `name_en` | `nullable, string, max:150` |
| `description_ar` | `nullable, string` |
| `price` | `required, numeric, min:0` |
| `cost_price` | `nullable, numeric, min:0` |
| `preparation_time` | `integer, min:1, max:120` |
| `sort_order` | `integer, min:0` |

```json
{
    "category_id": "{{category_id}}",
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "price": 45,
    "cost_price": 45,
    "preparation_time": 15,
    "sort_order": 1
}
```

---

#### `DELETE` /api/v1/menu/items/{item}

Destroy — menu/items/{item}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.delete`
- **Plan features:** _None_
- **Path params:** `{item}`

---

#### `GET` /api/v1/menu/items/{item}

Show — menu/items/{item}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.view`
- **Plan features:** _None_
- **Path params:** `{item}`

---

#### `PATCH` /api/v1/menu/items/{item}

Update — menu/items/{item}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{item}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `sometimes, string, max:150` |
| `name_en` | `nullable, string, max:150` |
| `description_ar` | `nullable, string` |
| `price` | `sometimes, numeric, min:0` |
| `cost_price` | `nullable, numeric, min:0` |
| `is_available` | `sometimes, boolean` |
| `preparation_time` | `sometimes, integer, min:1` |
| `sort_order` | `sometimes, integer, min:0` |
| `category_id` | `sometimes, integer` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "price": 45,
    "cost_price": 45,
    "is_available": true,
    "preparation_time": 15,
    "sort_order": 1,
    "category_id": "{{category_id}}"
}
```

---

#### `PUT` /api/v1/menu/items/{item}

Update — menu/items/{item}

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{item}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name_ar` | `sometimes, string, max:150` |
| `name_en` | `nullable, string, max:150` |
| `description_ar` | `nullable, string` |
| `price` | `sometimes, numeric, min:0` |
| `cost_price` | `nullable, numeric, min:0` |
| `is_available` | `sometimes, boolean` |
| `preparation_time` | `sometimes, integer, min:1` |
| `sort_order` | `sometimes, integer, min:0` |
| `category_id` | `sometimes, integer` |

```json
{
    "name_ar": "كشري",
    "name_en": "Koshary",
    "description_ar": "مثال",
    "price": 45,
    "cost_price": 45,
    "is_available": true,
    "preparation_time": 15,
    "sort_order": 1,
    "category_id": "{{category_id}}"
}
```

---

#### `DELETE` /api/v1/menu/items/{item}/photo

Delete Photo — menu/items/{item}/photo

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{item}`

---

#### `POST` /api/v1/menu/items/{item}/photo

Upload Photo — menu/items/{item}/photo

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{item}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `photo` | `required, image, mimes:jpeg,jpg,png,webp, max:5120` |

```json
{
    "photo": "مثال"
}
```

---

#### `PATCH` /api/v1/menu/items/{item}/toggle

Toggle — menu/items/{item}/toggle

- **Auth:** Bearer token + tenant header
- **Permissions:** `menu.update`
- **Plan features:** _None_
- **Path params:** `{item}`

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/orders

Index — orders

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `status` | `nullable, in:active,cooking,ready,completed,paid,cancelled` |
| `branch_id` | `nullable, integer` |
| `table_id` | `nullable, integer` |
| `channel` | `nullable, in:dine_in,qr,whatsapp,talabat,elmenus,own_delivery` |
| `fulfillment_type` | `nullable, in:dine_in,takeaway,delivery` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/orders

Create a new order with line items (dine-in, delivery, aggregator, etc.).

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.create`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `floor_table_id` | `nullable, integer` |
| `channel` | `required, in:dine_in,qr,whatsapp,talabat,elmenus,own_delivery` |
| `fulfillment_type` | `nullable, in:dine_in,takeaway,delivery` |
| `notes` | `nullable, string` |
| `delivery_address` | `nullable, string, max:500` |
| `customer_id` | `nullable, integer` |
| `items` | `required, array, min:1` |
| `items.*.menu_item_id` | `required, integer` |
| `items.*.quantity` | `required, integer, min:1` |
| `items.*.notes` | `nullable, string` |

```json
{
    "branch_id": "{{branch_id}}",
    "floor_table_id": "{{table_id}}",
    "channel": "dine_in",
    "fulfillment_type": "dine_in",
    "notes": "بدون بصل",
    "delivery_address": "١٢ شارع التحرير، الدقي، الجيزة",
    "customer_id": "{{customer_id}}",
    "items": [
        {
            "menu_item_id": 1,
            "quantity": 2,
            "notes": "كشري"
        },
        {
            "menu_item_id": 2,
            "quantity": 1,
            "notes": "فول"
        }
    ]
}
```

---

#### `GET` /api/v1/orders/{order}

Show — orders/{order}

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.view`
- **Plan features:** _None_
- **Path params:** `{order}`

---

#### `PATCH` /api/v1/orders/{order}

Update — orders/{order}

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.update`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `notes` | `nullable, string` |

```json
{
    "notes": "بدون بصل"
}
```

---

#### `PUT` /api/v1/orders/{order}

Update — orders/{order}

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.update`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `notes` | `nullable, string` |

```json
{
    "notes": "بدون بصل"
}
```

---

#### `POST` /api/v1/orders/{order}/items

Store — orders/{order}/items

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.update`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `menu_item_id` | `required, integer` |
| `quantity` | `required, integer, min:1` |
| `notes` | `nullable, string` |

```json
{
    "menu_item_id": "{{menu_item_id}}",
    "quantity": 2,
    "notes": "بدون بصل"
}
```

---

#### `DELETE` /api/v1/orders/{order}/items/{item}

Destroy — orders/{order}/items/{item}

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.update`
- **Plan features:** _None_
- **Path params:** `{order}`, `{item}`

---

#### `POST` /api/v1/orders/{order}/pay

Settle payment for an open order (cash, card, Vodafone Cash, split, etc.).

- **Auth:** Bearer token + tenant header
- **Permissions:** `payments.process`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `method` | `required, in:cash,card,vodafone_cash,instapay,meeza,valu,split` |
| `amount` | `required, numeric, min:0` |
| `cash_tendered` | `nullable, numeric, min:0` |
| `discount_type` | `nullable, in:percentage,fixed` |
| `discount_value` | `nullable, numeric, min:0` |
| `discount_reason` | `nullable, string, max:255` |
| `reference` | `nullable, string, max:100` |
| `splits` | `required_if:method,split, array, min:2` |
| `splits.*.method` | `required, in:cash,card,vodafone_cash,instapay,meeza,valu` |
| `splits.*.amount` | `required, numeric, min:0.01` |
| `splits.*.reference` | `nullable, string, max:100` |
| `loyalty_points` | `nullable, integer, min:1` |

```json
{
    "method": "cash",
    "amount": 45,
    "cash_tendered": 1,
    "discount_type": "percentage",
    "discount_value": 1,
    "discount_reason": "خصم موظف",
    "reference": "مثال",
    "splits": [
        {
            "method": "cash",
            "amount": 25,
            "reference": "مثال"
        },
        {
            "method": "vodafone_cash",
            "amount": 20,
            "reference": "مثال"
        }
    ],
    "loyalty_points": 50
}
```

---

#### `GET` /api/v1/orders/{order}/print/kitchen

Kitchen Ticket — orders/{order}/print/kitchen

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.view`
- **Plan features:** _None_
- **Path params:** `{order}`

---

#### `GET` /api/v1/orders/{order}/print/receipt

Receipt — orders/{order}/print/receipt

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.view`
- **Plan features:** _None_
- **Path params:** `{order}`

---

#### `POST` /api/v1/orders/{order}/refund

Refund a settled order payment.

- **Auth:** Bearer token + tenant header
- **Permissions:** `payments.refund`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `reason` | `nullable, string, max:255` |

```json
{
    "reason": "طلب العميل إلغاء الطلب"
}
```

---

#### `PATCH` /api/v1/orders/{order}/status

Update Status — orders/{order}/status

- **Auth:** Bearer token + tenant header
- **Permissions:** `orders.update`
- **Plan features:** _None_
- **Path params:** `{order}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `status` | `required, in:active,cooking,ready,completed,paid,cancelled` |

```json
{
    "status": "active"
}
```

---

#### `GET` /api/v1/reports/cash-summary

Cash Summary — reports/cash-summary

- **Auth:** Bearer token + tenant header
- **Permissions:** `reports.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `date` | `nullable, date` |
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/reports/daily

Daily — reports/daily

- **Auth:** Bearer token + tenant header
- **Permissions:** `reports.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `date` | `nullable, date` |
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/reports/top-items

Top Items — reports/top-items

- **Auth:** Bearer token + tenant header
- **Permissions:** `reports.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `start_date` | `nullable, date` |
| `end_date` | `nullable, date, after_or_equal:start_date` |
| `limit` | `nullable, integer, min:1, max:100` |
| `branch_id` | `nullable, integer` |

---

#### `GET` /api/v1/tables

Index — tables

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `section` | `nullable, string, max:50` |

---

#### `POST` /api/v1/tables

Store — tables

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.create`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `required, integer` |
| `name` | `required, string, max:20` |
| `section` | `nullable, string, max:50` |
| `capacity` | `integer, min:1, max:50` |
| `position_x` | `integer, min:0` |
| `position_y` | `integer, min:0` |

```json
{
    "branch_id": "{{branch_id}}",
    "name": "Downtown Branch",
    "section": "مثال",
    "capacity": 4,
    "position_x": 0,
    "position_y": 0
}
```

---

#### `DELETE` /api/v1/tables/{table}

Destroy — tables/{table}

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.delete`
- **Plan features:** _None_
- **Path params:** `{table}`

---

#### `GET` /api/v1/tables/{table}

Show — tables/{table}

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.view`
- **Plan features:** _None_
- **Path params:** `{table}`

---

#### `PATCH` /api/v1/tables/{table}

Update — tables/{table}

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.update`
- **Plan features:** _None_
- **Path params:** `{table}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:20` |
| `section` | `nullable, string, max:50` |
| `capacity` | `sometimes, integer, min:1` |
| `position_x` | `sometimes, integer, min:0` |
| `position_y` | `sometimes, integer, min:0` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name": "Downtown Branch",
    "section": "مثال",
    "capacity": 4,
    "position_x": 0,
    "position_y": 0,
    "is_active": true
}
```

---

#### `PUT` /api/v1/tables/{table}

Update — tables/{table}

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.update`
- **Plan features:** _None_
- **Path params:** `{table}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:20` |
| `section` | `nullable, string, max:50` |
| `capacity` | `sometimes, integer, min:1` |
| `position_x` | `sometimes, integer, min:0` |
| `position_y` | `sometimes, integer, min:0` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name": "Downtown Branch",
    "section": "مثال",
    "capacity": 4,
    "position_x": 0,
    "position_y": 0,
    "is_active": true
}
```

---

#### `PATCH` /api/v1/tables/{table}/status

Update Status — tables/{table}/status

- **Auth:** Bearer token + tenant header
- **Permissions:** `tables.update`
- **Plan features:** _None_
- **Path params:** `{table}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `status` | `required, in:free,occupied,reserved,unavailable` |

```json
{
    "status": "free"
}
```

---

### Platform

#### `POST` /api/v1/admin/auth/login

Login — admin/auth/login

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `email` | `required, email` |
| `password` | `required, string` |
| `device_name` | `nullable, string, max:100` |

```json
{
    "email": "owner@nilerestaurant.eg",
    "password": "SecurePass123!",
    "device_name": "تابلت الكاشير ١"
}
```

---

#### `POST` /api/v1/admin/auth/logout

Logout — admin/auth/logout

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/admin/auth/me

Me — admin/auth/me

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/admin/dashboard

Stats — admin/dashboard

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/admin/tenants

Index — admin/tenants

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `status` | `nullable, in:active,trial,grace_period,suspended` |
| `plan` | `nullable, in:starter,growth,pro,enterprise` |
| `search` | `nullable, string, max:100` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `POST` /api/v1/admin/tenants

Store — admin/tenants

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `restaurant_name` | `required, string, max:150` |
| `subdomain` | `nullable, string, max:50, alpha_dash` |
| `locale` | `nullable, in:ar,en` |
| `owner_name` | `required, string, max:100` |
| `owner_email` | `required, email, max:255` |
| `owner_password` | `required, string, min:8, max:100` |
| `owner_phone` | `nullable, string, max:20` |
| `branch_name` | `nullable, string, max:100` |
| `branch_name_ar` | `nullable, string, max:100` |
| `branch_address` | `nullable, string, max:255` |
| `branch_phone` | `nullable, string, max:20` |
| `timezone` | `nullable, timezone` |
| `plan` | `nullable, in:starter,growth,pro,enterprise` |
| `status` | `nullable, in:active,trial,grace_period,suspended` |

```json
{
    "restaurant_name": "مطعم النيل",
    "subdomain": "nilerestaurant",
    "locale": "ar",
    "owner_name": "محمد أحمد",
    "owner_email": "owner@nilerestaurant.eg",
    "owner_password": "SecurePass123!",
    "owner_phone": "+201012345678",
    "branch_name": "فرع وسط البلد",
    "branch_name_ar": "فرع وسط البلد",
    "branch_address": "شارع قصر النيل، وسط البلد، القاهرة",
    "branch_phone": "+201012345678",
    "timezone": "Africa/Cairo",
    "plan": "starter",
    "status": "active"
}
```

---

#### `GET` /api/v1/admin/tenants/{tenant}

Show — admin/tenants/{tenant}

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

---

#### `PATCH` /api/v1/admin/tenants/{tenant}

Update — admin/tenants/{tenant}

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:150` |
| `subdomain` | `sometimes, string, max:50, alpha_dash` |
| `locale` | `sometimes, in:ar,en` |
| `custom_domain` | `nullable, string, max:255` |

```json
{
    "name": "Downtown Branch",
    "subdomain": "nilerestaurant",
    "locale": "ar",
    "custom_domain": "menu.nilerestaurant.com"
}
```

---

#### `PATCH` /api/v1/admin/tenants/{tenant}/features

Update Features — admin/tenants/{tenant}/features

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `feature_flags` | `required, array` |
| `feature_flags.*` | `string, max:50` |

```json
{
    "feature_flags": []
}
```

---

#### `POST` /api/v1/admin/tenants/{tenant}/impersonate

Issue a short-lived tenant token for platform admin impersonation.

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

**Request body**

_None_

```json
{}
```

---

#### `PATCH` /api/v1/admin/tenants/{tenant}/plan

Update Plan — admin/tenants/{tenant}/plan

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `plan` | `required, in:starter,growth,pro,enterprise` |

```json
{
    "plan": "starter"
}
```

---

#### `PATCH` /api/v1/admin/tenants/{tenant}/status

Update Status — admin/tenants/{tenant}/status

- **Auth:** Admin Bearer token
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** `{tenant}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `status` | `required, in:active,trial,grace_period,suspended` |
| `trial_days` | `nullable, integer, min:1, max:365` |

```json
{
    "status": "active",
    "trial_days": 14
}
```

---

### Tenant

#### `GET` /api/v1/audit-log

Index — audit-log

- **Auth:** Bearer token + tenant header
- **Permissions:** `audit.view`
- **Plan features:** `audit_log`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `causer_id` | `nullable, integer` |
| `subject_type` | `nullable, string, max:255` |
| `per_page` | `nullable, integer, min:1, max:100` |
| `page` | `nullable, integer, min:1` |

---

#### `GET` /api/v1/branches

Index — branches

- **Auth:** Bearer token + tenant header
- **Permissions:** `branches.view`
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/branches

Store — branches

- **Auth:** Bearer token + tenant header
- **Permissions:** `branches.manage`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `required, string, max:100` |
| `name_ar` | `required, string, max:100` |
| `address` | `nullable, string, max:255` |
| `phone` | `nullable, string, max:20` |
| `timezone` | `nullable, timezone` |

```json
{
    "name": "Downtown Branch",
    "name_ar": "كشري",
    "address": "منطقة المعادي، القاهرة",
    "phone": "+201012345678",
    "timezone": "Africa/Cairo"
}
```

---

#### `PATCH` /api/v1/branches/{branch}

Update — branches/{branch}

- **Auth:** Bearer token + tenant header
- **Permissions:** `branches.manage`
- **Plan features:** _None_
- **Path params:** `{branch}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:100` |
| `name_ar` | `sometimes, string, max:100` |
| `address` | `nullable, string, max:255` |
| `phone` | `nullable, string, max:20` |
| `timezone` | `nullable, timezone` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name": "Downtown Branch",
    "name_ar": "كشري",
    "address": "منطقة المعادي، القاهرة",
    "phone": "+201012345678",
    "timezone": "Africa/Cairo",
    "is_active": true
}
```

---

#### `GET` /api/v1/ops/health

Health — ops/health

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/reports/branches/compare

Compare — reports/branches/compare

- **Auth:** Bearer token + tenant header
- **Permissions:** `reports.view`
- **Plan features:** `multi_branch`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `start_date` | `nullable, date` |
| `end_date` | `nullable, date, after_or_equal:start_date` |

---

#### `GET` /api/v1/settings

Show — settings

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `PATCH` /api/v1/settings

Update — settings

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:150` |
| `locale` | `sometimes, in:ar,en` |
| `custom_domain` | `nullable, string, max:255` |
| `whatsapp_phone_number_id` | `nullable, string, max:50` |
| `talabat_webhook_secret` | `nullable, string, max:255` |
| `elmenus_webhook_secret` | `nullable, string, max:255` |

```json
{
    "name": "Downtown Branch",
    "locale": "ar",
    "custom_domain": "menu.nilerestaurant.com",
    "whatsapp_phone_number_id": "+201012345678",
    "talabat_webhook_secret": "kitchen-secret-12345678",
    "elmenus_webhook_secret": "kitchen-secret-12345678"
}
```

---

#### `GET` /api/v1/settings/domain

Domain Status — settings/domain

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/settings/domain/verify

Verify custom domain DNS configuration for white-label access.

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/settings/eta

Show — settings/eta

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `PATCH` /api/v1/settings/eta

Update — settings/eta

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `eta_client_id` | `nullable, string, max:100` |
| `eta_client_secret` | `nullable, string, max:255` |
| `eta_taxpayer_id` | `nullable, string, max:100` |
| `eta_branch_id` | `nullable, string, max:20` |

```json
{
    "eta_client_id": "مثال",
    "eta_client_secret": "kitchen-secret-12345678",
    "eta_taxpayer_id": "مثال",
    "eta_branch_id": "مثال"
}
```

---

#### `DELETE` /api/v1/settings/eta/certificate

Delete Certificate — settings/eta/certificate

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/settings/eta/certificate

Upload Certificate — settings/eta/certificate

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `certificate` | `required, file, max:5120` |

```json
{
    "certificate": "مثال"
}
```

---

#### `GET` /api/v1/staff

Index — staff

- **Auth:** Bearer token + tenant header
- **Permissions:** `staff.view`
- **Plan features:** _None_
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `POST` /api/v1/staff

Store — staff

- **Auth:** Bearer token + tenant header
- **Permissions:** `staff.manage`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `required, string, max:100` |
| `email` | `nullable, email, max:255` |
| `phone` | `nullable, string, max:20` |
| `role` | `required, in:manager,cashier,waiter,cook,rider` |
| `branch_id` | `required, integer` |
| `password` | `nullable, string, min:8, max:100` |
| `pin` | `nullable, string, regex:/^\d{4}$/` |

```json
{
    "name": "Downtown Branch",
    "email": "owner@nilerestaurant.eg",
    "phone": "+201012345678",
    "role": "manager",
    "branch_id": "{{branch_id}}",
    "password": "SecurePass123!",
    "pin": "1234"
}
```

---

#### `GET` /api/v1/staff/shifts

Index — staff/shifts

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `user_id` | `nullable, integer` |
| `date` | `nullable, date` |

---

#### `GET` /api/v1/staff/shifts/active

Active — staff/shifts/active

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** _None_

**Query parameters**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |

---

#### `POST` /api/v1/staff/shifts/clock-in

Clock in staff member for the current shift.

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `branch_id` | `nullable, integer` |
| `notes` | `nullable, string, max:255` |
| `opening_float` | `nullable, numeric, min:0` |

```json
{
    "branch_id": "{{branch_id}}",
    "notes": "بدون بصل",
    "opening_float": 500
}
```

---

#### `POST` /api/v1/staff/shifts/clock-out

Clock out staff member and close the active shift.

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `notes` | `nullable, string, max:255` |
| `closing_cash_count` | `nullable, numeric, min:0` |

```json
{
    "notes": "بدون بصل",
    "closing_cash_count": 450
}
```

---

#### `GET` /api/v1/staff/shifts/current

Current — staff/shifts/current

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** _None_

---

#### `GET` /api/v1/staff/shifts/{shift}

Show — staff/shifts/{shift}

- **Auth:** Bearer token + tenant header
- **Permissions:** _None_
- **Plan features:** `staff_shifts`
- **Path params:** `{shift}`

---

#### `GET` /api/v1/staff/{staff}

Show — staff/{staff}

- **Auth:** Bearer token + tenant header
- **Permissions:** `staff.manage`
- **Plan features:** _None_
- **Path params:** `{staff}`

---

#### `PATCH` /api/v1/staff/{staff}

Update — staff/{staff}

- **Auth:** Bearer token + tenant header
- **Permissions:** `staff.manage`
- **Plan features:** _None_
- **Path params:** `{staff}`

**Request body**

| Parameter | Rules |
|-----------|-------|
| `name` | `sometimes, string, max:100` |
| `email` | `nullable, email, max:255` |
| `phone` | `nullable, string, max:20` |
| `role` | `sometimes, in:manager,cashier,waiter,cook,rider` |
| `branch_id` | `sometimes, integer` |
| `password` | `nullable, string, min:8, max:100` |
| `pin` | `nullable, string, regex:/^\d{4}$/` |
| `is_active` | `sometimes, boolean` |

```json
{
    "name": "Downtown Branch",
    "email": "owner@nilerestaurant.eg",
    "phone": "+201012345678",
    "role": "manager",
    "branch_id": "{{branch_id}}",
    "password": "SecurePass123!",
    "pin": "1234",
    "is_active": true
}
```

---

#### `POST` /api/v1/staff/{staff}/deactivate

Deactivate — staff/{staff}/deactivate

- **Auth:** Bearer token + tenant header
- **Permissions:** `staff.manage`
- **Plan features:** _None_
- **Path params:** `{staff}`

**Request body**

_None_

```json
{}
```

---

#### `GET` /api/v1/subscription

Show — subscription

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.view`
- **Plan features:** _None_
- **Path params:** _None_

---

#### `POST` /api/v1/subscription/cancel

Cancel — subscription/cancel

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.cancel`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/subscription/downgrade

Downgrade — subscription/downgrade

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.manage`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `plan` | `required, in:starter,growth,pro,enterprise` |

```json
{
    "plan": "starter"
}
```

---

#### `POST` /api/v1/subscription/downgrade/cancel

Cancel Pending Downgrade — subscription/downgrade/cancel

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.manage`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/subscription/resume

Resume — subscription/resume

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.cancel`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{}
```

---

#### `POST` /api/v1/subscription/upgrade

Upgrade — subscription/upgrade

- **Auth:** Bearer token + tenant header
- **Permissions:** `subscription.manage`
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `plan` | `required, in:starter,growth,pro,enterprise` |
| `gateway` | `required, in:paymob,fawry` |

```json
{
    "plan": "starter",
    "gateway": "paymob"
}
```

---

### Tenant · Onboarding

#### `POST` /api/v1/onboarding/register

Self-service restaurant onboarding. Creates tenant, owner account, and default branch. Subdomain is optional — auto-generated when omitted.

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

| Parameter | Rules |
|-----------|-------|
| `restaurant_name` | `required, string, max:150` |
| `subdomain` | `nullable, string, max:50, alpha_dash` |
| `locale` | `nullable, in:ar,en` |
| `owner_name` | `required, string, max:100` |
| `owner_email` | `required, email, max:255` |
| `owner_password` | `required, string, min:8, max:100` |
| `owner_phone` | `nullable, string, max:20` |
| `branch_name` | `nullable, string, max:100` |
| `branch_name_ar` | `nullable, string, max:100` |
| `branch_address` | `nullable, string, max:255` |
| `branch_phone` | `nullable, string, max:20` |
| `timezone` | `nullable, timezone` |
| `kitchen_device_secret` | `nullable, string, min:8, max:64` |

```json
{
    "restaurant_name": "مطعم النيل",
    "subdomain": "nilerestaurant",
    "locale": "ar",
    "owner_name": "محمد أحمد",
    "owner_email": "owner@nilerestaurant.eg",
    "owner_password": "SecurePass123!",
    "owner_phone": "+201012345678",
    "branch_name": "فرع وسط البلد",
    "branch_name_ar": "فرع وسط البلد",
    "branch_address": "شارع قصر النيل، وسط البلد، القاهرة",
    "branch_phone": "+201012345678",
    "timezone": "Africa/Cairo",
    "kitchen_device_secret": "kitchen-secret-12345678"
}
```

---

### Webhooks

#### `POST` /api/v1/webhook/aggregators/elmenus

Elmenus — webhook/aggregators/elmenus

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "order_ref": "ELM-554433",
    "status": "accepted",
    "customer_phone": "+201098765432"
}
```

---

#### `POST` /api/v1/webhook/aggregators/talabat

Talabat — webhook/aggregators/talabat

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "external_id": "TBL-998877",
    "status": "new",
    "items": [
        {
            "name": "كشري",
            "quantity": 2,
            "price": 45
        }
    ]
}
```

---

#### `POST` /api/v1/webhook/fawry

Handle — webhook/fawry

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "merchantRefNumber": "ORD-001",
    "paymentStatus": "PAID",
    "paymentAmount": 150
}
```

---

#### `POST` /api/v1/webhook/paymob

Handle — webhook/paymob

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "type": "TRANSACTION",
    "obj": {
        "id": 123456,
        "success": true,
        "amount_cents": 15000
    }
}
```

---

#### `DELETE` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `GET` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `OPTIONS` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

---

#### `PATCH` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "object": "whatsapp_business_account",
    "entry": [
        {
            "changes": [
                {
                    "value": {
                        "messages": [
                            {
                                "from": "201012345678",
                                "text": {
                                    "body": "أريد طلب كشري"
                                }
                            }
                        ]
                    }
                }
            ]
        }
    ]
}
```

---

#### `POST` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "object": "whatsapp_business_account",
    "entry": [
        {
            "changes": [
                {
                    "value": {
                        "messages": [
                            {
                                "from": "201012345678",
                                "text": {
                                    "body": "أريد طلب كشري"
                                }
                            }
                        ]
                    }
                }
            ]
        }
    ]
}
```

---

#### `PUT` /api/v1/webhook/whatsapp

Handle — webhook/whatsapp

- **Auth:** None
- **Permissions:** _None_
- **Plan features:** _None_
- **Path params:** _None_

**Request body**

_None_

```json
{
    "object": "whatsapp_business_account",
    "entry": [
        {
            "changes": [
                {
                    "value": {
                        "messages": [
                            {
                                "from": "201012345678",
                                "text": {
                                    "body": "أريد طلب كشري"
                                }
                            }
                        ]
                    }
                }
            ]
        }
    ]
}
```

---
